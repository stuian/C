# include <stdio.h> 
# include <math.h>

int main()
{
	// 600851475143
	
	int num	= 600851475143;
	int i = 2;
	int max = 1;
	_Bool flag = 1; 
	
	half = (int)sqrt(num);
	for (i;i<=half;i++)
	{
		if (num % i == 0)
		{
			if (i>max)
			{
				half-2 = (int)sqrt(i);
				for (i;i<=half;i++)
				{
					if (num % i == 0)
					{
						;
					}
					else
					{
						;
					}
				}	
				max = i;	
			}
		}
	}
	
	while (flag)
	{
		factor = num / max; 
	}	
	
	//判断是否是质数 
	half = (int)sqrt(num);
	for (i;i<=half;i++)
	{
		if (num % i == 0)
		{
			;
		}
		else
		{
			;
		}
	}
}
